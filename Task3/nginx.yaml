- name: Deploy customized Nginx pages on AWS and Azure
  hosts: all
  become: yes
  vars:
    # Detect cloud based on group membership
    cloud: "{{ 'aws' if 'aws' in group_names else 'azure' }}"

  tasks:
    - name: Copy customized index.html for each cloud
      copy:
        src: "index-{{ cloud }}.html"
        dest: /var/www/html/index.html
        owner: root
        group: root
        mode: '0644'

    - name: Ensure Nginx is installed
      apt:
        name: nginx
        state: present
        update_cache: yes

    - name: Ensure Nginx config serves /var/www/html/index.html
      lineinfile:
        path: /etc/nginx/sites-available/default
        regexp: '^\s*index\s'
        line: '    index index.html;'
        state: present
      notify: Restart nginx

  handlers:
    - name: Restart nginx
      service:
        name: nginx
        state: restarted
